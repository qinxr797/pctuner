# 电脑调优助手 · 使用说明

**版本 v3.0「全人群版」**（2026-09-24）

给「电脑用了几年感觉变慢了、想调一调但不知道从哪下手」的人用的工具。

---

## v3.0 这次更新了什么

### 不再只服务 FPS 玩家

前两版整个工具围着竞技射击转，每个优化项下面写的都是「对 CS2 的影响」。
**不玩 FPS 的人打开会觉得这工具跟自己没关系** —— 这一版就是改这个。

把「三个 FPS 游戏」扩成**八种使用场景**：
竞技射击（CS2 / 无畏契约 / 三角洲）、大型单机 3A、网游挂机、
轻度游戏 / 模拟器、不玩游戏（办公上网）、老机器救急。

**关键是：同一项对不同的人，结论经常是相反的。**
比如「关闭 SysMain」——
网游玩家「推荐」、老机器用户「慎用」（机械盘靠它预读，关了开程序更慢）、
CS2 玩家「中性」。全部 55 个优化项 × 8 种场景的结论都写全了。

### 首页预设重做

第一排现在是「**按用途选**」：

| 预设 | 给谁 | 和别的档有什么不一样 |
|---|---|---|
| 大型单机 3A | 黑神话、艾尔登法环 | **不关全屏优化**（这点和 FPS 预设相反，3A 的 HDR 和 Alt+Tab 靠它） |
| 网游 / 挂机 / 多开 | 梦幻、剑网3、原神 | 重点在网络延迟；**不动 CPU 时间片**（多开会让后台窗口更卡） |
| ★ 不玩游戏 | 办公上网刷视频 | 一条游戏相关的都没有，全是每天能感觉到的；**全是低风险项** |
| 老机器救急 | 内存小 / 机械盘 | **刻意避开在老机器上帮倒忙的那些**，见下 |

老机器那一档特别说明 —— 这几条在老机器上是**反的**，所以故意不放进去：
关内存压缩、关页面组合、关 SysMain、关核心停放、取消启动项延迟、关快速启动。

### 界面动画

加了淡入和过渡动画，用的是 WPF 原生能力，没有引入任何第三方 UI 库
（那会毁掉「解压双击就能跑、零依赖」这个优点）。

**可以在「个性化」页整体关掉。** 这工具本来就服务配置吃紧的机器，
动画不该反过来变成负担。关了之后所有切换瞬间完成，功能一模一样。

---

## v2.0 更新了什么

上一版有人反馈「调了几项，游戏帧数没变」。这个反馈是对的，
所以这一版专门冲着这件事做了四块东西：

### 1. 新增「帧数瓶颈诊断」——先回答「为什么没变」

系统体检页多了一个按钮：**★ 为什么我帧数没变？**

它不给建议，只摆事实，逐条告诉你每个影响帧数的环节现在是什么状态。
结论通常是这三种之一：

- **你的瓶颈在硬件/BIOS 上**，软件碰不到 ——
  单通道内存、XMP 没开、独显没启用、温度墙降频。
  这几条是最常见的真凶，而且改注册表一辈子也修不好
- **那些真正影响帧数的项，你机器上本来就已经是最优的** ——
  点了当然不会变
- **你开的那些项本来就不改帧数**，改的是手感和后台占用

> 实话：本工具几十个开关里，**真能让帧数数字变大的只有五六条**。
> 其余的改的是延迟、卡顿、后台占用。诊断页会把这件事说清楚。

### 2. 新增「激进优化」分类（11 项）

往最深层走的一组，**每一条都明确写了代价**，用不用自己判断：

| 项目 | 效果 | 代价 |
|---|---|---|
| 关闭 Spectre/Meltdown 缓解 | **老 CPU 上 5%~30%，这组里最大的一条** | 理论上的侧信道攻击风险 |
| 关闭 Defender 实时保护 | 加载/读图明显变快 | **机器等于裸奔**（建议先用下面那条） |
| 关闭 Defender 云查杀 | 卡顿改善明显 | 只损失新病毒的检出率，本地防护还在 |
| 关闭 CPU 电源限流 | 笔记本上效果明显 | 功耗温度上升 |
| 关闭核心停放 + 禁止空闲 | 降低唤醒延迟 | 功耗温度明显上升，散热差的反而更慢 |
| 显卡 MSI 中断模式 | 帧生成更平滑 | 极少数老硬件不稳 |
| 恢复全局定时器精度 | 要实测，有人有效有人没有 | 略增功耗 |
| 禁止自动覆盖显卡驱动 | 防止帧数某天突然掉下来 | 要自己记得更新驱动 |
| 延长显卡超时判定 | 治「显示驱动已停止响应」 | 真死锁时要多等 8 秒 |
| 关闭内存页面组合 | 省一点 CPU | 内存占用上升，8G 机器别开 |
| 关闭错误报告 | 崩溃后不再卡几十秒 | 没有 dump 可排查 |

**这一组全部默认不勾，也不进任何预设。** 自己看完说明再决定。

### 3. 新增「自带软件」页 —— 微软预装应用怎么处理

列出系统预装的 UWP 应用，每个都写清楚是干什么的，给三档结论：

- **可以删** —— 广告位和废弃功能（天气、资讯、Office 推广壳、
  混合现实、Skype、小娜……）
- **看情况** —— 取决于你用不用（照片、截图工具、Xbox、WSL……）
- **必须留** —— 删了会出事的，**界面上直接勾不上**

> 网上那些「一键卸载所有自带应用」的脚本最容易翻车的地方，
> 是把应用商店、安全中心界面、运行库、视频解码器一起删了。
> 这个页面有一道硬黑名单专门拦这些。
>
> 另外卸载**只针对当前用户、不动系统镜像**，
> 任何一个删错了都能去 Microsoft Store 搜名字装回来。

### 4. 新增「个性化」页 —— 换肤

六套内置纯色皮肤（暖灰 / 雾霾蓝 / 鼠尾草绿 / 奶茶棕 / 石墨深色 / 午夜蓝），
也可以**选自己的图片当背景**，配一个不透明度滑块调图片和文字的平衡。

选了什么下次打开自动记住。

> 换肤只改中性色和主色。**绿/黄/红那几个状态标签的颜色是故意不跟着变的**
> ——「高危」永远是红的，不能因为换了皮肤看错。

---

## 怎么打开

双击 **`一键启动.bat`** → 弹出「用户账户控制」时点「**是**」。

就这一步。不用安装任何东西（用的是 Windows 自带的 PowerShell），
不联网，不后台常驻，关掉窗口就等于退出。

> **万一双击了没反应 / 窗口一闪就没**
> 双击旁边的 **`诊断启动.bat`**。正常那个启动器会把黑框藏起来，
> 出错信息跟着一起没了；诊断版不藏窗口、跑完也不关，
> 报错会留在屏幕上，截图发过来就能定位。

### 报「对路径的访问被拒绝」怎么办

这是微信/QQ 传过来的文件最常见的一个坑。

**原因**：Windows 会给从网络收到的文件打一个「来自网络，不安全」的
隐藏标记（就是右键属性里那个「解除锁定」）。带着这个标记的脚本，
PowerShell 直接拒绝读取 —— 文件明明在，就是读不了。

**程序已经会自动处理**：从 v2.0 开始，启动时会自动清除这个标记，
一般碰不到这个错了。

如果还是遇到，按顺序试：

1. 右键 **`电脑调优助手.zip`（压缩包本身）** → 属性 →
   勾「解除锁定」→ 确定 → **重新解压一遍**
   （关键：要对 **zip** 解锁再解压。对解压出来的文件夹解锁没用。）
2. 查杀毒软件的「隔离区」。这个工具要读注册表、查开机启动项、
   扫描弹窗来源，行为和病毒很像，360 / 火绒 / 电脑管家
   **经常误报并锁住文件**。找到后「恢复」+「添加信任」。
3. 换个位置放，比如 `D:\PCTuner`。别放「下载」文件夹、桌面、
   U 盘或微信接收目录。
4. 实在不行就临时退出杀毒软件，用完再开。

> 出错窗口本身会告诉你是哪一种情况（文件缺失 / 空文件 /
> 被拒绝访问 / 被占用），按它说的做就行。

> 为什么要管理员权限？因为改电源计划、系统服务、注册表 HKLM 分支
> 都必须是管理员。不给权限的话工具什么都做不了。

---

## 第一次用，按这个顺序来

### 1️⃣ 先看「系统体检」页

**不要一上来就乱勾优化项。** 先看体检结论。

体检会按「性价比从高到低」排序，红色的「严重」条目是真正影响
这台机器的问题。很多时候你会发现——最该做的事根本不是改注册表，
而是「显卡驱动两年没更新了」或者「该清灰换硅脂了」。

软件优化的天花板就那么高。体检页会诚实地告诉你天花板在哪。

### 2️⃣ 再去「垃圾清理」页

点「**扫描可清理的垃圾**」，等它跑完，看看各项能清多少。
默认勾好的都是安全项，直接点「开始清理选中项」就行。

一共 22 类，常见的大头：回收站、临时文件、浏览器缓存、显卡着色器缓存、
Windows 升级残留（`$Windows.~BT` 那些隐藏文件夹）、显卡驱动安装包残留、
微信/QQ 缓存、Windows.old、WinSxS 组件仓库。

> 清理前把浏览器、Steam、微信、QQ 关掉，不然那些文件被占用删不掉。

> **微信那一项只清纯缓存**（小程序缓存等），聊天记录、聊天图片、
> 收到的文件**一律不碰**——那是你的资料不是垃圾。微信占几十个 GB 的话，
> 用微信自己的「设置 → 文件管理 → 清理微信存储空间」挑着删。

### 2️⃣.5 「日常维护」页 —— 平时就用这一页

这一页是给**平时**用的，不像性能优化那样是一次性的：

- **一键日常维护**：清垃圾 + 刷新 DNS + 优化系统盘，一条龙。
  每个月点一次就行，不会改任何性能设置
- **每周自动清理**：勾上之后建一个计划任务，每周日中午 12 点
  在后台静默跑一遍推荐的清理项，不弹窗不打扰
- **磁盘优化**：自动识别固态/机械——固态做 TRIM 恢复写入速度，
  机械做碎片整理。**不会对固态做碎片整理**（那只会白白消耗寿命）
- **硬盘健康与寿命**：读 SMART，告诉你固态写入寿命用了百分之多少、
  通电多少小时、有没有坏道迹象。老机器最怕硬盘悄悄坏掉
- **微信 / QQ 占用**：只统计不删，告诉你它们吃了多少空间
- **大文件查找**：回答「我 C 盘到底被什么占满了」。列出最大的 40 个文件，
  点一下直接在资源管理器里定位。**只列出来，不会自动删任何东西**
- **显示器刷新率一键拉满**：买了高刷屏却还跑在 60Hz 非常常见（换线、
  重装驱动、接新屏都会退回去）。列出这套「显示器+线+显卡」能跑的所有
  刷新率，一键切换。切完弹 15 秒倒计时确认框 —— **万一黑屏什么都别动，
  倒计时结束会自动切回去**
- **快捷小工具**：刷新 DNS 缓存、重启资源管理器、系统磁盘清理等
  藏得很深的功能

> 快捷键：`Ctrl+F` 跳到搜索框，`F5` 重新检测当前页，`Esc` 清空搜索。
> 「系统体检」页的「导出诊断报告到桌面」会把硬件信息、体检结论、
> 优化项状态、操作日志打包成一个 txt —— 朋友电脑出问题，让他导一份
> 发过来，比来回问十句话快得多。

> 顺带：游戏里画面卡死、显卡驱动假死时，按 **Win + Ctrl + Shift + B**
> 可以直接重启显卡驱动，屏幕黑一下就恢复，不用重启电脑。
> 这是 Windows 自带的快捷键，很多人不知道。

### 2️⃣.8 「弹窗排查」页 —— 治「黑框一闪而过」

**症状**：黑色的框突然弹出来，里面什么都没有，一闪就没了，
有时候一下子弹四五个。

**那是什么**：那是控制台窗口（cmd / powershell / cscript）。
有程序在后台调用命令行，但没把窗口藏好，你就看到一个黑框闪过。
框里没东西是因为它执行得太快。

**怎么用**：

1. 点「**开始扫描**」。工具会把所有"能在后台执行命令"的地方扫一遍：
   计划任务、开机自启、系统服务、WMI 事件订阅、Winlogon、映像劫持
2. 带 **⚡ 会弹黑框** 标记的就是嫌疑人 —— 点「只看会弹黑框的」直接过滤
3. 每条都写清楚了它是什么、为什么可疑、能不能关
4. 勾掉复选框 = 禁用（**不是删除**，随时能勾回来）

**判断「会不会弹黑框」的依据**（工具逐条检查）：

| 条件 | 说明 |
|---|---|
| 跑的是控制台程序 | cmd / powershell / cscript / .bat。注意 `wscript` 不弹框，`cscript` 才弹 |
| 跑在你的登录会话里 | 计划任务设成"不管用户是否登录都运行"的话在后台会话，看不见 |
| 没加隐藏窗口参数 | 有 `-WindowStyle Hidden` 的**恰恰不会弹** |

三个条件缺一个就不弹。

**左边扫不出来？用右边的「抓现行」——这才是终极手段**

左边扫的是"开机会自动跑什么"。但弹窗**不一定来自自启项**：
最难查的一种是某个**已经在运行的程序**（加速器、录屏、驱动工具、
破解补丁…）每隔一阵子自己开一个 cmd 去干点什么。这个父程序本身
在任何自启位置都看起来完全正常，扫破天也扫不到。

对付这种只有一个办法：**直接盯「新建进程」这件事本身**。
不管元凶藏在哪，它要弹窗就必须创建进程，跑不掉。

| 方式 | 怎么用 | 适合 |
|---|---|---|
| **① 实时监控** | 点「开始监控」→ 正常用电脑 → 黑框一出现，这里立刻多出记录，带**父进程** | 弹得比较勤，蹲得到 |
| **② 持续记录** | 开启后关掉工具也在记，事后回来「查看进程记录」，带**完整命令行** | 不定时，蹲不到 |
| **③ 任务记录** | 看任务计划程序的运行历史 | 怀疑是计划任务时 |

橙色标记的是**控制台进程**（黑框本身），看它的「**父进程**」——
那就是真正开出黑框的程序。

> ②会改两处系统设置（审核策略 + 一个注册表值），都已备份，
> 点「关闭持续记录」或用「全部还原」随时恢复。排查完记得关掉，
> 开着的时候每创建一个进程都会写一条安全日志。

**最常见的元凶**（工具会自动认出来并标注身份）：

- `OfficeBackgroundTaskHandler` —— Office 后台任务，**"每隔几分钟闪一个黑框"的头号元凶**
- `user_feed_synchronization` —— 老 Windows 的 RSS 同步
- Chrome / Edge / Adobe / NVIDIA 的更新检查器
- 各种「管家」「优化大师」「驱动精灵」
- 品牌机厂商的检测工具

**这不是杀毒软件**。它找的是"持久化驻留点"和"会弹黑框的东西"，
不做病毒特征比对。扫出「高危」的话，正确做法是用 Windows Defender
或火绒做一次全盘扫描，而不是只把它禁用了事。

### 3️⃣ 然后「启动项管理」页

把没必要开机自启的关掉。每一项下面都写了「这个能不能关、关了会怎样」。
绿色「可关」的放心关，红色「建议保留」的别动。

**关闭是可逆的，不删除任何文件**，随时能勾回来。

### 4️⃣ 最后才是「性能优化」页

页面顶部有一排**游戏预设**，玩 FPS 的直接点 **「★ 三合一 FPS 通用」**，
它会自动勾好对 CS2 / 无畏契约 / 三角洲行动 三个游戏都有好处的项目，
然后点左下角「**应用选中的优化**」。

不玩这几个游戏的话，点「勾选通用推荐项」就行。

想自己挑，就一条条点开看说明。**每一条都写清楚了：这是干什么的、
对三个游戏分别有什么影响、代价是什么、出问题怎么还原。**
看不懂的就别勾，这是最安全的原则。

---

## 🎯 FPS 玩家专用说明（CS2 / 无畏契约 / 三角洲行动）

### 五个预设

| 预设 | 说明 |
|---|---|
| **★ 三合一 FPS 通用** | 18 项，三个游戏都受益。**三个都玩就选这个** |
| CS2 竞技向 | 三合一 + CPU 时间片调度（需要自己实测） |
| 无畏契约国服 | 三合一（强调 ACE 反作弊的启动要求） |
| 三角洲行动 | 三合一 + 关闭 MPO（改善画面撕裂/闪烁） |
| 保守模式 | 只做零风险项目，不碰任何安全相关设置 |

**设计原则：所有预设都只做「三个游戏都不吃亏」的事。**
每个游戏的预设都是三合一底座的**超集**——只往上加，绝不拿掉别的游戏
需要的项目。任何可能让其中一个变差的（比如全局关闭全屏优化），
一律不放进预设，只留给你自己单独测。

### 最重要的一件事：ACE 反作弊的虚拟化要求

**无畏契约国服和三角洲行动用的都是腾讯 ACE 反作弊**（不是 Vanguard）。
ACE 要独占 CPU 虚拟化来对抗 DMA 硬件外挂，所以它的要求是**两个相反的方向**：

```
BIOS 里    →  虚拟化要【开】  Intel: VT-x + VT-d   AMD: SVM + IOMMU
Windows 里 →  内存完整性(HVCI)、Hyper-V 要【关】
```

不满足就弹「CPU虚拟化未开启或被其他软件占用」，**游戏根本进不去**。
这是腾讯游戏安全中心官方文档写明的要求，不是玄学。

好消息是：**这三个游戏在这件事上没有冲突**。
CS2 用的是 VAC，不管这些，而且第三方实测关掉内存完整性后
CS2 平均帧数约 **+25 帧**——是单项收益最大的 Windows 设置。
两个游戏硬性要求 + 一个游戏最大收益，方向完全一致。

「系统体检」页会自动检测这四项，缺哪个告诉你哪个。
Windows 里那两项工具能直接帮你关；BIOS 那两项得你自己开机按 Del/F2 进去开。

> ⚠ **唯一的例外**：如果哪天去玩**国际服** Valorant（用 Riot 自家的
> Vanguard），要求正好**相反**——必须开着内存完整性，关了会报
> `VAN: RESTRICTION - HVCI`。真要玩国际服，把「关闭内核隔离」
> 这一项单独还原回去就行，其他项目不受影响。
> 工具在勾选时会主动提醒你这一点。

### 顺便纠正一个流传很广的错误

网上教程里的 `Win32PrioritySeparation = 38`（0x26），
在 Win10/11 桌面版上**等于什么都没改**——38 的含义（短时间片 + 动态 +
前台 3 倍加成）恰好就是系统的默认行为。很多「一键优化工具」这一步是
纯粹的安慰剂。真正和默认不同的是 **40（0x28）**，本工具用的是 40，
而且默认不勾，因为它因机而异，必须自己测 1% Low 才知道有没有用。

### 5️⃣ 重启一次

有些项目（标着「需要重启生效」）要重启才起作用。

---

## 万一改坏了怎么办

这个工具有**三层保险**：

| 保险 | 说明 |
|------|------|
| **单项还原** | 点开某一项 → 「只还原这一项」 |
| **全部还原** | 性能优化页底部 →「全部还原为系统默认」，一键回到没用过这个工具的状态 |
| **系统还原点** | 默认每次应用优化前自动创建。实在不行就在「设置 → 系统 → 恢复」里整体回滚 |

### 还原用的是「你自己机器的原值」

这一点和市面上的「优化大师」不一样：

本工具**改任何东西之前，先把你机器上的原值原封不动存进
`Backup\original-values.json`**。还原时读的是这个文件，
而不是套用网上抄来的所谓"默认值"。

所以 **`Backup` 文件夹不要删**，删了就只能退回通用默认值了。

---

## 它会做什么 / 不会做什么

### ✅ 会做

- 改注册表里的性能相关开关（每一处都先备份）
- 改电源计划、少数几个 Windows 服务的启动类型
- 删除临时文件和缓存（路径全部写死在代码里）
- 读取硬件信息做体检

### ❌ 不会做

- 不联网、不上传任何数据、不装驱动、不装任何软件
- 不后台常驻、不弹广告（只有你主动开「每周自动清理」时才会建一个计划任务）
- 不动你的文档、图片、下载、游戏存档、**聊天记录**
- 不动浏览器的收藏夹、密码、Cookie、历史记录（只删缓存）
- 不超频、不改电压、不刷 BIOS
- **不做「内存优化 / 一键释放内存」** —— 这是故意的。那类功能的原理是
  强制把程序的内存赶到硬盘上，任务管理器数字确实降了，但程序下次用到
  就得从硬盘读回来，结果是更卡不是更快。纯粹的视觉安慰剂，所以不做。
  内存真不够就去「系统体检」页看加内存的建议。

---

## 关于"效果"的实话

工具里每一项都标了预期效果，而且是**诚实标的**：

- 标「明显」的（关 Game DVR、电源计划、视觉特效、菜单延迟）——
  是真的能感觉到
- 标「小幅」的 —— 就是小幅，别指望脱胎换骨
- 标「因机而异」「建议实测」的 —— 意思是它可能有用也可能没用，
  改完自己跑个游戏对比一下
- 有一项（Prefetch 清理）直接标了「**不推荐清**」，因为它是
  被优化软件误传成垃圾的有用数据

**别指望改几个注册表能把五年前的机器变成新机器。**

v2.0 的「★ 为什么我帧数没变？」按钮会针对你这台机器具体算一遍，
比下面这个通用清单更准。但通用结论还是这四件事：

1. 换固态硬盘（如果还在用机械盘）——提升最大，没有之一
2. 加内存 / 组双通道
3. 更新显卡驱动
4. 清灰换硅脂（老机器「玩一会儿就开始卡」基本都是这个）

---

## 文件说明

```
PCTuner\
├── 一键启动.bat        ← 双击这个
├── 诊断启动.bat        出问题时用这个，能看见报错
├── PCTuner.ps1          主程序（界面）
├── 使用说明.md          本文件
├── Modules\
│   ├── Engine.ps1       备份/还原引擎（最核心的安全机制）
│   ├── Tweaks.ps1       所有优化项的定义和详细说明
│   ├── Games.ps1        三个 FPS 游戏的适配层 + 预设定义
│   ├── Cleaner.ps1      垃圾清理规则（22 类）
│   ├── Maintain.ps1     日常维护工具（磁盘/健康/大文件/自动清理）
│   ├── Inspect.ps1      弹窗排查 / 可疑驻留项检测
│   ├── Appx.ps1         自带应用清单 + 硬黑名单
│   ├── Theme.ps1        换肤
│   ├── SysInfo.ps1      硬件检测和体检逻辑
│   └── Startup.ps1      启动项管理
└── Backup\             ← 别删！还原功能靠它
    ├── original-values.json   你机器的原始设置
    └── log-*.txt              操作日志
```

所有代码都是纯文本的 PowerShell，用记事本就能打开看。
里面写满了中文注释，不放心的话可以自己逐行检查它到底做了什么。

---

## 常见问题

**Q：双击 bat 没反应 / 一闪而过？**
A：右键 bat →「以管理员身份运行」。如果还是不行，说明系统的
PowerShell 执行策略被安全软件锁死了，去杀毒软件里放行一下。

**Q：提示「创建还原点失败」？**
A：系统保护被关了。控制面板 → 系统 → 系统保护 → 选中 C 盘 →
配置 → 启用系统保护。不影响工具使用（工具自己有完整的备份机制）。

**Q：清理完发现某个软件要重新登录？**
A：不应该发生——清理只删缓存目录。如果确实遇到了，
在日志页看一下清了哪些项，告诉我一声。

**Q：应用了优化但没感觉到变化？**
A：先确认有没有重启（标「需要重启生效」的项目必须重启）。
另外看一眼体检页——如果那里有红色的「严重」条目，
那才是瓶颈所在，注册表优化解决不了。

**Q：能给别人的电脑用吗？**
A：可以，整个文件夹拷过去就行。每台机器的备份是独立的，
存在各自的 `Backup` 文件夹里。

---

## 🌐 浏览器吃内存 / 刷视频卡（Edge 专项）

「性能优化」页新增了**浏览器**分类，5 项，改的全是
[微软官方组策略](https://learn.microsoft.com/deployedge/microsoft-edge-policies)，
不是网上流传的魔改注册表。

**12 项，按代价分三档**，预设栏里「浏览器瘦身」一行直接点：

| 档 | 内容 | 代价 |
|---|---|---|
| **① 温和** | 关后台常驻、睡眠标签页 5 分钟、关侧边栏/购物助手、确保硬件加速、清空新标签页资讯流 | 几乎没有。侧边栏没了、新标签页变干净，就这些 |
| **② 深度**（推荐） | ① + 关 Drop/工作区/网页截图/推广位、关资源投放、效率模式常开、关预加载 | 那几个功能本身没了。右键截图可用 Win+Shift+S 代替 |
| **③ 极致** | ② + **同站共用进程**（`--process-per-site`，实测省约 25% 内存） | 隔离性下降：某网站一个标签页崩，**同网站所有标签页一起崩** |

三档是**递进包含**的，选 ② 就自动含 ①。

另外**两项故意没打包进任何预设**，要用自己去列表里勾：
- 关组件更新 —— 会导致 Widevine 不更新，某些正版视频网站以后可能放不了
- 关 SmartScreen —— 省的资源微乎其微，但这是普通用户最后一道防线。**不建议关**

### 关于 --process-per-site 的局限

Edge 没有「设置启动参数」的官方策略，这一项是**改快捷方式**实现的
（桌面 / 开始菜单 / 任务栏），原有参数会完整保留。

⚠ 只有**从这些快捷方式启动**才生效。在开始菜单搜「edge」回车走的
不是快捷方式，参数不生效 —— 这点工具绕不过去。

> 副作用：设了组策略后，Edge 设置页里对应开关会变灰显示「由你的组织管理」。
> 这是正常的，不是中毒。用「还原」清掉策略就恢复可点。

### 刷抖音卡死 —— 先说清楚这不是浏览器的锅

抖音网页版是**已知的内存泄漏大户**：视频强制播放、无限滚动的 DOM 只增不减。
泄漏到一定程度，V8 垃圾回收越来越频繁，每次都阻塞主线程 ——
表现就是「点一下整个卡住」。

**换浏览器治不好这个**，换 Chrome / Thorium / Brave 都一样，因为根因在抖音的网页代码。

能真正缓解的三招：

1. **把抖音装成独立应用**：Edge 里打开抖音 → 右上角 `…` → 应用 →
   「将此站点安装为应用」。之后它是独立进程独立窗口，
   卡了单独关掉重开，**不会拖垮你其它标签页**
2. **刷久了就关掉重开**——泄漏的内存随进程退出一次性还回来，比任何优化都彻底
3. **确认硬件加速在用**：地址栏输 `edge://gpu`，看 `Video Decode` 是不是
   `Hardware accelerated`。不是的话 1080p 视频在用 CPU 软解，老机器必卡

### 关于「换个极简浏览器」

查过了，**在 Chromium 世界里「极简」和「省内存」不是一回事**：

- **Thorium**：提速靠的是编译期优化（AVX2 / LTO / PGO），官方 README 通篇讲
  执行速度，**没有一条是关于内存的**。扩展兼容 Chrome 商店
- **Ungoogled-Chromium**：砍掉 Google 服务，后台任务确实少了，
  但**扩展要手动下 CRX 装**，对不折腾的人不友好

根本原因：**一个标签页一个进程**是 Chromium 的核心安全设计，
谁 fork 都改不了。能装 Chrome 扩展 = 必须是 Chromium 内核 = 必然多进程吃内存。
这两个要求在技术上是冲突的。

所以顺序应该是：**先把 Edge 瘦身（上面 5 项）→ 再看内存够不够**。
如果机器只有 8GB，加一条内存（一两百块）比换任何浏览器都管用 ——
「系统体检」页会直接告诉你内存多大。