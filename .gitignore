# ===== 绝对不能上传的：本机私有数据 =====
#
# Backup/ 里是「这台机器」的东西，不是程序的一部分：
#   · original-values.json —— 改设置前备份的原始注册表值，
#     里面含网卡 GUID 这类能标识具体机器的信息
#   · log-*.txt            —— 操作日志，含本机用户名和路径
#   · theme.json           —— 个人的皮肤选择
#   · skin/                —— 用户自己选的背景图
#
# 这些是运行时生成的，别人克隆下来会自己生成自己的。
Backup/

# ===== 打包产物 =====
*.zip

# ===== 开发/调试临时文件 =====
*.log
_*/
scratch/
tmp/

# 截图默认不传，但 docs/ 下面的是 README 要用的，放行
*.png
!docs/*.png

# ===== Windows 自己丢的垃圾 =====
Thumbs.db
desktop.ini
$RECYCLE.BIN/
